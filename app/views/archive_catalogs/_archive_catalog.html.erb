  <li class="icon-folder">
    <a href="<%= browse_archive_catalog_path(:archive_catalog_urn => archive_catalog.urn) %>" rel="explr-ajax" class="citable"><%= line_break_title(archive_catalog.label, 5).html_safe %></a>

    <% if archive_catalog.children.any? %>
      <ul <%= archive_catalog.children.include?(@catalog) ? "class ='explr-expand'" : ''%> >
        <%= render :partial => "archive_catalog", :collection => archive_catalog.children %>
      </ul>
    <% end %>
</li>


