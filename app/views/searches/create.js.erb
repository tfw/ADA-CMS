<% if @search.valid? %>
	$("#save-search-response").html("Saved").fadeOut(5000);
	$("#save-search-area").hide();
<% else %>
	$("#save-search-response").html(@search.error.messages.first).fadeOut(5000);
<% end %> 
