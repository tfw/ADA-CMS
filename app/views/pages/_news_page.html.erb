<article>

  <% if @archive_news.any? or @archive_studies.any? %>
  	<aside class="breakout">
      <%= render(:partial => 'pages/archive_news', :object => @archive_news)%>
    </aside>
  <% end %>
  
  <%= news_page.body.html_safe if news_page.body %>
</article>

