<% if resource.errors.any? %>
    <h2><%= pluralize(resource.errors.count, "error") %> prohibited this page from being saved:</h2>
    <ul>
    <% resource.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
<% end %>

<dl>
 <dt>Content Type<dd><%= @media.asset_content_type %></dd>
 <dt>File Size<dd><%= number_to_human_size(@media.asset_file_size) %></dd>
 <dt>Created<dd><%= time_ago_in_words(@media.created_at) %> ago </dd>
 <dt>Thumbnail<dd><%= image_tag @media.asset.url(:thumb) %></dd>
</dl>

<%= link_to 'Delete', staff_media_path(@media), :method => :delete %>
<%= link_to 'Back to Resources index', staff_media_index_path %>
