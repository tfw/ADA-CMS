<% content_for(:breadcrumbs) do %>
	<%= @archive.name %>: Integrations
<% end %>


<h2>Integrations<h2>

<h3>Studies<h3>

Add an integration:

  <%= semantic_form_for([:staff, @archive, @archive_study_integration]) do |form| %>
    <%= form.hidden_field :archive_id, :value => @archive.id %>
    <%= form.hidden_field :user_id, :value => current_user.id %>
		http://bonus.anu.edu.au:80/obj/fStudy/au.edu.anu.assda.ddi: 
    <%= form.input :label, :input_html => {:size => "100"} %>
		<%= form.commit_button %>
	<% end %>
	
<% for integration in @archive.archive_study_integrations %>
  <%= integration.url %>
  <% if integration.archive_study %>
    
  <% end %>
<% end %>

<h3>Study Queries<h3>

<h3>Study Blacklist<h3>