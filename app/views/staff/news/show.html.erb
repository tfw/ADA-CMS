<% content_for(:breadcrumbs) do %>
  News Show
<% end %>

<div class="news">
  <h2><%= @news.title %></h2>
  <%= raw(@news.body) %>
  <p>(In archive<%= @assigned_archives.size > 1 ? 's' : ''%> <%= @assigned_archives.map(&:name) * ',' %>)
  </p>
  <!-- tag cloud -->
  <%= link_to 'Edit', edit_staff_news_path(@news) %>
  <%= link_to 'Back to News index', staff_news_index_path %>
</div>
